# shellcheck shell=bash

source_up

dotenv $(dirname $(realpath ${BASH_SOURCE[0]}))/.env
dotenv_if_exists

use nix

# FIXME: nix-direnv
# only watches default.nix by default
# https://github.com/nix-community/nix-direnv#tracked-files
# nix_direnv_watch_file shell.nix pyproject.yaml

watch_file pyproject.yaml
